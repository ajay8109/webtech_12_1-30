// remove the first element  , store it in a variable, and print both the removed value and the updated value

let arr = ["Apple", "banana", "Mango"]

let remove = arr.shift();
console.log(remove);
console.log(arr);